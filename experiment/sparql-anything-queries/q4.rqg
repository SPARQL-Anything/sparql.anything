PREFIX source: <urn:facade-x:ns#>

SELECT DISTINCT ?id
WHERE{

	SERVICE <facade-x:data/COLLEZIONI_PALAZZO_MADAMA_marzo2017.json> {
		?s source:Materiali ?material .
		?s source:Inventario ?id .
		FILTER(REGEX(?material,".*bronzo.*","i"))
	}
}
