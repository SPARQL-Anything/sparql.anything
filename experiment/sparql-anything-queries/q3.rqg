PREFIX source: <urn:facade-x:ns#>

SELECT DISTINCT ?titolo
WHERE{

	SERVICE <facade-x:data/COLLEZIONI_FONDO_GABINIO_MARZO_2017.json> {
		?s source:Datazione ?date .
		?s source:Titolo ?titolo .
		FILTER(REGEX(?date,".*1935.*","i"))
	}
}
